# Example External Method Repo

This tiny fake repository represents a research baseline method that trains a regression model from a CSV dataset.

It is intentionally small and exists only to test PaperPilot-Agent local repository inspection.
